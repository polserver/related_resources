use uo;
use os;
use cfgfile;
include "include/convert";
program magekiller(parms)
local who := parms[1];

while(1)
	sleep(1);
	lowermana(who, 3);
	lowerstam(who, 1);
	eraseobjproperty(who, "#ra");
	eraseobjproperty(who, "#mr");
endwhile

endprogram