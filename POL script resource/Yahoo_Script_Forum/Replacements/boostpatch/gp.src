use uo;

program textcmd_gp (who, text)

 	local item := Target( who );
                  local prop := GetObjProperty(item, text);
                  SendSysMessage(who, text + " : " +  prop);
                  
         
endprogram